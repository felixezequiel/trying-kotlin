package reservations.infrastructure.persistence

/**
 * Contexto de banco de dados para o serviço de reservas. Por enquanto usa armazenamento em memória.
 * Será substituído por implementação real (PostgreSQL, etc).
 */
class DatabaseContext {
    // Placeholder para configuração de banco de dados
    // Será implementado quando houver necessidade de persistência real
}
